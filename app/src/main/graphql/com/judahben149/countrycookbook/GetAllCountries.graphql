query GetAllCountries($continentCode: ID!) {
    continent(code: $continentCode) {
        code
        name
        countries {
            code
            name
            capital
            emoji
        }
    }
}
